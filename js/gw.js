gw.mainObject = function(width, height, canvas = null) {
  this.width = width;
  this.height = height;

  if (canvas != null) {
    this.canvas = 
  }
}

gw.mainObject.prototype.setWidth(width) {
  this.width = width;
}